const name = `Ezichi Amarachi`; // Name
const id = `HNG-05820`; // HNG ID
const language = `javascript`; // Language

console.log(
  `Hello World, this is ${name} with HNGi7 ID ${id} using ${language} for stage 2 task`
);
