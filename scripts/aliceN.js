let details = {
  myName: 'Alice Ndeh',
  id: 'HNG-00642',
  stack: 'JavaScript',
  email: 'alicendeh16@gmail.com',
};

const { myName, id, stack, email } = details;

console.log(
  `Hello World, this is ${myName} with HNGi7 ID ${id} and email ${email} using ${stack} for stage 2 task`
);