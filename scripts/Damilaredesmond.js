// Create a function that accepts the following parameters
function intern(name, id, email, language) {
  return `Hello World, this is ${name} with HNGi7 ID ${id} and email ${email} using ${language} for stage 2 task`;
}

// Call the function and assign to a variable
let val = intern(
  "Damilare Desmond Adebowale",
  "HNG-02035",
  "damilareadebowale93@gmail.com",
  "JavaScript"
);

// console.log the variable
console.log(val);
