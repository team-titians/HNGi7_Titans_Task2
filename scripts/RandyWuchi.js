// Script to return my HNG Id, Name, Email, language
const name = `Nnadozie Randy Nnawuchi`; // Name of Intern
const id = `HNG-04436`; // HNG ID
const language = `javascript`; // Script language
const email = `nnadoziennawuchi@yahoo.com`; // Email 

console.log(`Hello World, this is ${name} with HNGi7 ID ${id} and email ${email} using ${language} for stage 2 task`);