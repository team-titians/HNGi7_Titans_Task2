// Declare the intern method
const internDetails = () => {
    // declare the intern variables
    const name = 'Zachary Moseti';
    const id = 'HNG-05091';
    const email = 'mosetizachary001@gmail.com';
    const language  = 'JavaScript';
    return `Hello World, this is ${ name } with HNGi7 ID ${ id } and email ${ email } using ${ language } for stage 2 task`
}
// call the function and output the details
console.log(internDetails());
